# TalentIQ — Agentic RAG Pipeline

# Core Web & Auth
sentence-transformers
fastapi
uvicorn[standard]
python-multipart
passlib[bcrypt]
argon2-cffi
bcrypt==4.0.1
python-jose[cryptography]
pydantic[email]>=2.11.9,<2.13
email-validator
python-dotenv
jose
jwt

# Database
sqlalchemy
sqlalchemy-utils
SQLAlchemy
psycopg2-binary

# AI & RAG (Optimized for CPU)
torch>=2.4.0 #--index-url https://download.pytorch.org/whl/cpu
transformers>=4.44.0
langchain
langchain-community
langchain-groq
langchain-openai  # optional — only needed if AI_PROVIDER=openai is set (core/ai_provider.py); Groq remains the default and this import is lazy
groq
pypdf
pymupdf
chromadb

# Multi-agent screening committee (Bulk Screening / Talent Pool only —
# never used for interview orchestration, which stays on LangGraph).
# litellm is required for CrewAI's LLM() wrapper to recognize the "groq/"
# model prefix and reuse the same GROQ_API_KEY as core/llm.py.
crewai
litellm

# Background Tasks
celery[redis]==5.3.6
redis==5.0.1
websockets>=12.0

# Email
fastapi-mail==1.4.1
sentry-sdk[fastapi]==2.19.0
alembic==1.14.0
posthog==3.7.4
reportlab==4.2.5